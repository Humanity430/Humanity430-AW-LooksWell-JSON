# JSON 글쓰기 평가 리포트 UI

AI 글쓰기 평가 결과 JSON을 카드형 리포트로 변환해 보여주는 정적 웹페이지입니다.

`index.html`을 브라우저로 열고, 실제 평가 JSON을 직접 붙여 넣거나 JSON 파일을 첨부해 사용합니다.

점수는 역할 설정·2단계 질문·3단계 질문 각 10점, 자기 언어화 70점을 기준으로 표시하며 총점은 100점입니다. 입력 데이터는 서버로 전송하거나 저장하지 않고 현재 브라우저 화면에서만 처리됩니다.
